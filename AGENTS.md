# Agents instructions

- do not run tests after every change
- do not run `./build.sh` after every change
- always follow the Single Responsibility Principle; avoid cramming too much functionality into too
  few files
- add new files to `project.pbxproj` so Xcode builds them properly